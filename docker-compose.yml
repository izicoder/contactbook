version: "3.8"

services:
    frontend:
        build:
            context: ./frontend
            dockerfile: ./frontend.dockerfile
        ports:
            - "5173:5173"
        depends_on:
            - backend

    backend:
        build:
            context: ./backend
            dockerfile: ./backend.dockerfile
        ports:
            - "5555:5555"
